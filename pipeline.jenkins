pipeline {
   agent any

   stages {
     stage('build') {
        steps {
            echo "FULL WORKSPACE = $WORKSPACE"
            echo "WSPACE_PATH = ${WSPACE_PATH}"
            bat "\"${tool 'MSBuild'}\" WindowsFormsApp2.sln"
        }
     }
   }
}
